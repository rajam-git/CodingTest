
public class Problem2_Recursion {
	public String reverseString(String inputStr) {
		String retStr = new StringBuffer(inputStr).reverse().toString());
		//Requirements: reverse the input string by using recursion.
		
		return retStr;
	}
}
